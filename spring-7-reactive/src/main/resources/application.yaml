server:
  port: 8082
spring:
  application:
    name: spring-7-reactive
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

#logging:
#  level:
#    org.springframework.r2dbc: trace

debug: true