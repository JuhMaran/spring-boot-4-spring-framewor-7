## 30. Introduction
Okay. We successfully created a spring Boot web application. From there, we are going to jump into dependency injection. Dependency injection is really a fundamental thing about spring. Very very powerful tool. It allows you to inject dependencies into your application at runtime, and you can actually compose behavior of your application by what gets injected. So this is a very powerful concept. This is one of the fundamental things that you really need to get a good grasp on with spring frameworks. So we are going to be doing a deep dive into that. Then we'll start looking at RESTful Web Services and one of my favorites, Project Lombok. And then further down the road we are going to start building RESTful web services using spring MVC. So but first we are going to jump into dependency injection. This is really fundamental spring stuff because from here, everything else in the course we will be utilizing the concepts of dependency injection with the spring framework.

## 31. SOLID Principles of OOP
In this video, we are going to talk about the solid principles of OOP or object oriented programming. Now the history of solid principles goes back to March of 1995. They were originally concepts by Robert Martin, also known as uncle Bob. He started as a variety of writings and blog posts, and then they later became the foundation of Martin's book called Agile Software Development Principles, Patterns and Practices. And then Michael Feathers is actually credited with coining the acronym Solid. Now what is solid? Let's take a closer look at that. These are actually five principles. We have a single responsibility principle. Open closed principle. Liskov substitution principle. Interface segregation principle. And then finally dependency inversion principle. So single responsibility principle. So just because you can doesn't mean you should. One of my favorite sayings there. So in the single responsibility principle every class should have a single responsibility. There should never be more than one reason for a class to change. Your classes should be small. Ideally, a good guideline to go by is no more than a screenful of code. Avoid air quotes, God classes, things that do. Everything's split big classes into smaller classes. I have seen Java methods that are over 2000 lines, and the developers swore that they testified that these were complex. So if you have a method that's 2000 lines of code, something's wrong. So don't don't be that developer making these massive methods 2000 line method is absolutely impossible to test properly. You want small, digestible chunks of code basically is what we're saying with a single responsibility principle. Next on the list is open closed principle. So here we're saying open chest surgery is not needed when putting on a coat. For example, here in the open closed principle we are saying that your classes should be open for extension but closed for modification. You should be able to extend the classes behavior without modifying it. So here we want to be using private variables with getters and setters only when you need them, and a good habit here is to be using abstract base classes. So the principle here is designing your software in a way in your classes that can be extended for their use. Next is Liskov substitution principle. If it looks like a duck, quacks like a duck, but needs batteries and you probably have the wrong abstraction. So this was actually founded by Barbara Liskov in 1998. So object oriented programming goes back quite some time. Objects in a program would be replaceable with their instances of their subtypes without altering the correctness of the program. So here if you violate this the violations will fail. That is a test. So if you remember some of your object oriented programming principles, the foundations of using that as a test. A square is a rectangle. However a rectangle is not a square. So very, very simple. Test there to follow that substitution principle. Next we have the interface segregation principle. Want me to plug this in? Where. So this is a very important principle. Make fine grained interfaces that are specific. Many client specific interfaces are better than one general purpose interface. You want to keep your components focused to minimize the dependencies between them. And then notice the relationship here to single responsibility principle. So you want to avoid God interfaces. And we already saw this a little bit in the course where I talked about the dependency injection of the spring framework where we want to be using interfaces. So what this is saying is that we'll have interfaces that we're coding to, to allow us to use Spring Framework to change the behavior of the application by plugging in different components. So that's really the concept behind integration segregation principle. You want your components basically be plug and play. So you don't want to over complicate them, but allow yourself the flexibility where you can swap things in and out. If you get something too complex, then you won't be able to have componentized software dependency inversion principle. Would you solder a lamp directly to the electrical wiring of the wall? No, of course not. You'd want to use a plug. So this principle is saying that abstraction should not depend upon the details, and details should not depend upon the abstractions. This is important that higher level and lower level objects depend upon the same abstraction. This is much different than dependency injection. Easy to confuse the two. Dependency injection is how objects obtain dependent objects. This is saying that your abstraction should work together and not be tightly coupled. So you don't want to have something tightly coupled where the two objects are dependent upon each other. You want that abstraction. That abstraction should allow the objects to work independently, but also be like plug and play so you can move things in and out like a think of a wall socket. A wall socket is going to accept a lamp, a radio, a TV, so it will accept different things, but through that air quotes of a socket interface. So in summary, the solid principles of OOP will lead you to write better quality code. Your code will be more testable and easier to maintain. A key theme throughout all these principles is to avoid tight coupling your code. So think about that 2000 line method. That's just insane, to be honest with you. It's just a bunch of code. A lot of it would be unreachable from unit testing. So it's just a bad, very fragile piece of software. But at the same time, be pragmatic when you're following the solid principles. Every request path does not need its own controller class. So you can have a controller for products that is going to handle the save update delete operations. So the class does have a purpose in life technically would violate the single responsibility principle, but like I'm saying, be pragmatic when we implement these. If your class does have four methods that are all related to a relatable object, that is perfectly fine. So again, be pragmatic, but also follow these principles. You do want to have a purpose in life for the classes that you write, and the less coupling that you have between the classes, the more flexibility and more quality your code will be.

### History of SOLID Principles of OPP
- The SOLID Principles of OOP date back to March of 1995
- The principles are from Robert Martin (aka Uncle Bob)
- Started as writings and blog posts
- Later became the foundation of Martin’s book “Agile Software Development: Principles, Patterns, and Practices”
- Michael Feathers is credited with establishing the SOLID acronym

### SOLID Principles of OPP
- **S** - Single Responsability Principle
- **O** - Open/Closed Principle
- **L** - Liskov Substitution Principle
- **I** - Interface Segregation Principle
- **D** - Dependency Inversion Principle

### SRP - Single Responsability Principle
Just beacuse you *can doesn’t* mean you *should*.
- Every Class should have a single responsibility.
- There should never be more than one reason for a class to change.
- Your classes should be small. No more than a screen full of code.
- Avoid ‘god’ classes.
- Split big classes into smaller classes.
- My 2000 line method tested “fine”

### OCP - Open Closed Principle
Open Chest Surgery Is Not Needed When Putting On A Coat
- Your classes should be open for extension
- But closed for modification
- You should be able to extend a classes behavior, without modifying it.
- Use private variables with getters and setters - ONLY when you need them.
- Use abstract base classes

### LSP - Liskov Substitution Principle
If It Looks Like A Duck, Quacks Like A Duck, But Needs Batteries - You Probably Have The Wrong Abstraction
- By Barbara Liskov, in 1998
- Objects in a program would be replaceable with instances of their subtypes WITHOUT altering
the correctness of the program.
- Violations will often fail the “Is a” test.
- A Square “Is a” Rectangle
- However, a Rectangle “Is Not” a Square

### ISP - Interface Segregation Principle
You Want Me To Plug This In, Where?
- Make fine grained interfaces that are client specific
- Many client specific interfaces are better than one “general purpose” interface
- Keep your components focused and minimize dependencies between them
- Notice relationship to the Single Responsibility Principle?
- ie avoid ‘god’ interfaces

### DIP - Dependency Inversion Principle
Would you solder a lamp directly to the electrical wiring in a wall?
- Abstractions should not depend upon details
- Details should depend upon abstractions
- Important that higher level and lower level objects depend on the same abstract interaction
- This is not the same as Dependency Injection - which is how objects obtain dependent objects

### Summary
- The SOLID principles of OOP will lead you to better quality code
- Your code will be more testable and easier to maintain
- A key theme is avoiding tight coupling in your code
- Be pragmatic when following SOLID
    - Every request path does not need its own controller class

## 32. The Spring Context
In this section of the course, we are going to be exploring dependency injection using the spring framework. The first thing we really need to talk about is the spring context. I'm going to do a demonstration of that here. And what I want to do is I'm going to create a new package called controllers. And I'm just going to call this my controller. So new Java class, we will be adding this into git. And I'm going to use the stereotype spring stereotype of controller. And even though we don't have a web dependency on the classpath, the spring web starter is not there. So we don't have Tomcat. The stereotype controller still will create this as a spring bean, so I'm just creating a mock controller here. Or a fake controller I guess would be a better way to say that. And here I'm going to create a method that's going to return back a string. We'll just say hello. Doesn't take anything in. And Here is due system out. I'm in the controller and we'll say return. So we're just going to return back. Hello everyone with our controller method. Now what we can do here in our spring application. Again because we do not have Tomcat when we run this it will simply start and then stop. But the spring application here, when we do run it, you can see that this does return back an application context. So I'm going to modify this say. Context. Equals that. And the way Spring Boot works is it's going to do a annotation scan for annotating components within this. And actually I made a mistake here. This needs to be actually under here. Otherwise we would not see that. So very important about the default behavior of spring Boot As I configure spring to do component scan for any annotated components in the package that it's in and below. When I had it up one level in the Spring Framework package, it would not have seen it because it's not in that current package here. This will work because it is in that in that path. So now what I can say is my controller equals I'm going to ask the context and say been the get been is actually the overloaded. You can see it being. So we can do by type class name or name. Let's come in here and say that my controller class. And also system out the controller say hello. What we're doing here is on the main method. We are asking for the application context. And then from the context this is the spring context that gets loaded up and configured with spring bean. being so, spring is going to create that bean for us. Put it into the context, and that context is going to hold on to it. And when we need it for things like dependency injection, the context provides it. So here I'm going directly to the context and asking for an instance of my controller. So let's go ahead and run this now. So here you can see the output on the console says in main method. So that's from this. And then in my controller when we call the say hello we do have I'm in the controller. And then this is the string that is returned. And we are doing the system out for that in the main method. So this is a very simple example of how spring, the spring context sees that controller that has been annotated, that indicates that it is a spring bean. And the context creates an instance of. It holds that in the context. And when it is asked for it, it provides that that component that's in the spring context.

## 33. Spring Test Context
Okay. In the previous video, I showed you how to get contacts from the main class. We can also work with that within the test context. So here the spring boot spring initializer creates this test here. So it's a JUnit5 test and it's annotated with spring boot test. And that annotation tells the test to create a spring context. And what we can do here is because this is a spring managed component, we can say application context. If we annotate this with auto wired, this is going to tell the spring context to inject a context for us. And. Come in here we'll write a new test. Get controller from context. And here what we can do just like we did previously let's say my controller. Equals application context. And we're going to say get bean and we say my controller class. That and now we can say do system out. Say hello and let's go ahead and run that. And when we run that spring boot is going to run. It's doing a component scan picks up my controller and that is going to be inside the spring context again. And we can see here take a look at the controller. Hello everyone. And also because I'm testing we can also do that directly here. We can say my controller. And we'll also say auto wired. Now I'm going to create another test I minimize this output window. And what we're doing here is now we're asking spring to auto wire in the application context and the controller. So when this test runs, the spring context is going to be utilized to provide those instances. So now I do system out. So here now we're going to be taking that my controller. And we'll just do say hello out of that test method I'm going to come in the gutter and just click on this arrow here to run just that test method. And let's resize this. And I did it. Okay. So this here exception. So that's coming up. The test context is not seeing the servlet context. That's because we did not put the web context on that. So that looks like it's a behavior right out of spring. So that's kind of interesting that it's complaining about that. So the spring test context says it can't start web context. And it's kind of an unusual situation that we have just spring there. So that is okay that it can't see that. Give me some complaints there. The the default spring configuration. However, our test did run as expected. So you can see here that I am getting the console output as expected, and I can write some assertions in the test. Make sure that that sale is returning back the proper string. But the purpose here for the lesson, I want to show you two methods here. The first was through the application context which is being auto wired. This is dependency injection working from spring. So spring is injecting the application context. And I'm asking it for an instance of the controller. And the second case I'm saying inject a controller instance. So we're doing the same thing two different ways. So one we're asking the application context for it. The second we are telling spring to auto wire that being into our test and then execute the test against that. So two different ways of doing the same thing. But here we are seeing spring's dependency injection in action, providing us beans that are inside the spring context.

## 34. Basics of Dependency Injection
In this video we're going to talk about the basics of dependency injection. We've already been seeing this some in the course, and let's take a closer look at it. So this is a quote from John I hope I don't say the name John Munsch. I hope I'm saying his name correctly there. So dependency injection for five year olds. “When you go and get things out of the refrigerator for yourself, your can cause problems. You might leave the door open, you might get something Mommy or Daddy doesn’t want you to have. You might even be looking for something we don’t even have or which has expired. What you should be doing is stating a need, ‘I need something to drink with lunch’, and then we will make sure you have something when you sit down to eat.” So this is really kind of the concept of dependency injection. You are saying in your classes that you need this object to work with, so you're basically declaring that you need it for your class to function properly, and you don't worry about how it is provided to you. So it's an important concept how that works. So dependency injection is where a needed dependency is injected by another object or another program. It can be at instantiation via constructor, or after the object is created via a setter, or in some cases not optimal, but direct property access. The class being injected really has no responsibility in instantiating that object that is being injected. Some people say that you avoid declaring objects using the word new. I've seen newer developers believe that this is a rule that you need to work on. I say this not 100% correct. There are going to be instances where you do create a new object. Just when you start getting into dependency injection, be pragmatic in your approach of what is and is not being managed in the spring context. You don't need to manage every single object in the spring context. So be pragmatic in the approach of using dependency injection. It is a powerful tool, but it can also be overused. Types of dependency injection by class properties that is the least preferred. These can be public or private properties. Using private properties. You can do this. I considered it evil. You can see there I have that in red. If you have a private property, spring can actually use reflection to go ahead and set that at runtime. This air quotes works, but it is slow and it also makes your testing difficult. It becomes a kind of a headache to test classes where you are performing dependency injection on private properties by setters. This used to be an area that most people preferred and going from old memories, I think spring was a little temperamental using constructors in the early days, but that is much past. There's been a lot of debate in the community about this. By constructor is the most preferred now, because when you are instantiating the object to create the object, you need to pass in that dependency via constructor. So the object cannot exist without that dependency being injected into it. So concrete classes versus interfaces. Dependency injection can be done with either. Generally you want to avoid specific concrete classes. Dependency injection via interfaces is highly preferred. It allows that runtime to decide the actual implementation of the interface to inject. So this file is that integration segregation principle of the solid paradigm. And it also makes your code more testable. So if you are injecting an interface, if you need to mock that at test time, that becomes a very trivial to do. It's very easy to do that. So your life will be much easier if you're using interfaces for your dependencies. Inversion of control. This is a concept that some people struggle with. Inversion control. It's also known as IOC. This is a technique to allow dependencies to be injected by runtime. The dependencies are not predetermined. This really allows your framework to compose the application by controlling which implementation is injected. So a great example. We've already seen in the class that how we used a H2 in-memory data source, it would be trivial for me to reconfigure that application using MySQL data source. And the only difference there is that we'd be injecting in a MySQL data source over H2 in-memory data source, and the application function either way, but one would be talking to an H2 database, the other would be talking to MySQL. So in version control it's important to remember the control of the dependency is being converted over to the managing framework, which in this case would be spring frameworks and version of control. This is another quote by Ralph Johnson and Brian Foote.  “One important characteristic of a framework is that the methods defined by the user to tailor the framework will often be called from within the framework itself, rather than from the user's application code. The framework often plays the role of the main program in coordinating and sequencing application activity. This inversion of control gives frameworks the power to serve as extensible skeletons. The methods supplied by the user tailor the generic algorithms defined in the framework for a particular application.”  So what they are saying here, in a nutshell, is that the framework is going to provide a lot of work, and you are not going to work with the framework itself directly very much. However, you will be focusing your development efforts not on the framework but on the application code. And the framework will then assemble your application for you. So very, very important concept here where we are working in conjunction with the framework to build our application, but we are not really calling framework. It's very rare that you need to actually call a spring framework method or class. The framework does a lot of the heavy lifting for us now in version control versus dependency injection, these are really easy to confuse, especially if you're new to this type of programming. So dependency injection really refers to much of the composition of your classes. So you compose the classes you are writing with dependency injection in mind. You might even write code to inject a dependency for a unit test. I'm going to demonstrate that coming up. So the act of injecting a dependency is technically dependency injection. Inversion control is the runtime environment of your code. So you are taking your application code, putting it into the Spring Framework runtime environment. The controller dependency injection is then converted to the framework. So spring is going to be dependencies are injected. For example, you could have an application that has an H2 in-memory database and a MySQL database and that data source that you could set up a switch as to which one gets injected. So your application will run on both, but at runtime you decide which database you want to be talking to. Now, some best practices with dependency injection always favor using constructor injection over setter and especially properties. So when you set up a dependent component, the ideal is to use final properties for the injected components, declare the property private and final, and then initialize it in the constructor. So that is the ideal and whatever practical code and interface. And then finally, not everything needs to be performed with dependency injection. If it's something trivial, I recommend just being pragmatic about it, like a date format, or you don't want to be doing that unless you need that date format universally available in your application. So small things like that really don't get a lot of benefit. But something like a data source definitely does get benefit from using dependency injection coming up in the course. We are going to explore these topics a lot deeper.

### Dependency Injection for 5 years olds
> “When you go and get things out of the refrigerator for yourself, your can cause problems. You might leave the door open, you might get something Mommy or Daddy doesn’t want you to have. You might even be looking for something we don’t even have or which has expired. What you should be doing is stating a need, ‘I need something to drink with lunch’, and then we will make sure you have something when you sit down to eat.”
> *John Munsch, 28 October 2009*

### DI - Dependency Injection
- Dependency Injection is where a needed dependency is injected by another object.
- Can be at instantiation via constructor, or after via setter
- The class being injected has no responsibility in instantiating the object being injected.
- Some say you avoid declaring objects using ‘new’
- Not 100% correct…
- Be pragmatic in what is and is not being managed in the Spring Context

### Types of Dependency Injection
- By class properties - least preferred
- Can be public or private properties
- Using private properties is **EVIL**
    - Spring can use reflection to set private properties
    - “Works” but is slow & makes testing difficult
- By Setters - Area of much debate
- By Constructor - Most Preferred

### Concrete Classes vs Interfaces
- DI can be done with Concrete Classes or with Interfaces
- Generally DI with Concrete Classes should be avoided
- DI via Interfaces is highly preferred
    - Allows runtime to decide implementation to inject
    - Follows Interface Segregation Principle of SOLID
    - Also, makes your code more testable - mocking becomes trivial

### IoC - Inversion of Control
- Inversion of Control - aka IoC
- Is a technique to allow dependencies to be injected at runtime
- Dependencies are not predetermined
- Allows the framework to compose the application by controlling which implementation is injected
    - Example - H2 in memory data source or MySQL data source

> “One important characteristic of a framework is that the methods defined by the user to tailor the framework will often be called from within the framework itself, rather than from the user's application code. The framework often plays the role of the main program in coordinating and sequencing application activity. This inversion of control gives frameworks the power to serve as extensible skeletons. The methods supplied by the user tailor the generic algorithms defined in the framework for a particular application.”
> *Ralph Johnson and Brian Foote*

### Inversion of Control (IoC) vs Dependency Injection (DI)
- IoC and DI are easily confused
- DI refers much to the composition of your classes
    - ie - you compose your classes with DI in mind
    - You might write code to ‘inject’ a dependency
- IoC is the runtime environment of your code
    - Control of Dependency Injection is inverted to the framework
    - Spring is in control of the injection of dependencies

### Best Practices with Dependency Injection
- Favor using Constructor Injection over Setter Injection
- Use final properties for injected components
- Declare property `private final` and initialize in the constructor
- Whenever practical, code to an interface

## 35. Dependecies without Injection
Okay, in this video, I'm going to show you an example of without performing dependency injections, something that might typically happen. So let's say our greeting here the hello everyone. That was a let's say that was something that we had to get from a database or some type of service, for example. So we're not just going to have a string. We want to externalize how we get that greeting out of the controller class here. I'm saying we got some business logic behind that. We're not going to really define what that is, but we do want to get that from a different component. So if I come up here, I'm going to create a new package and we'll just call it services. And in this I'm going to create a new interface. And what we'll do is I'll just return back a string. Say greeting doesn't take any parameters like so. And let's also complete the implementation of that. So I'm just doing option enter to bring up that dialog interface. And we are going to do greeting service simple say greeting. And we will add that to get. And here I just want to say. Hello everyone from base service. So nothing too complex there. And then in my controller, let's say that I do need to have that I could do something like private final greeting service and then also create a constructor for that. And I'm not showing you the optimal way right now. And I'm not going to inject that into the. And here I'm just going to initialize it say new greeting service implementation. So this is a concrete implementation of this. It's very rigid. And what I'm doing here I'm going to say greeting service say greeting. So here I'm going to write a quick test for this. So here I'm just going to say Junit five create controller test. Say hello. And here I can say my controller equals new. My controller. Say hello. And now when I run that, what's going to happen is that class is going to get created. You can say say hello everyone from the base service. So we are mocking, not really mocking. We are creating a dependency here on the greeting service, but internal to the controller. Right now the controller has all the control as to how that greeting service gets created and how it is managed. So right now this controller does not play nicely with Spring framework. This is really the way that we do not want to be doing things. But I want to show you the concept here where we're taking a dependency. External dependency that is going to be giving us our greeting. And this is one way that people might do it without the spring framework and without using dependency injection. So coming up in the course, we are going to be looking at different and better techniques to utilize that greeting service. But in the context of using dependency injection and within the context of using spring framework. 

## 36. Dependency Injection without Spring
Okay. In the previous video, we refactored the controller to use a greeting service. But this is a very non flexible implementation. So what we are going to do is explore dependency injection but without using the spring framework. We will do things manually. So I'm going to come in and create some new controllers. And in this one I will call this a property injected controller. And in this I'm just going to have simply a greeting service. Call it say hello just to be consistent with my controller. And in this case it's going to say greeting service. Say greeting if you want to return that. So for this to work let's write a quick test for that. And we'll do a setup on that. And also say hello. Here we'll say property controller. So we're going to first we need to set that and then here for the property. So I just set the greeting service equals new greeting service. Simple. And now we can just do a simple cyst out. And I'm not doing any test assertions because the greeting I'm just going to show the console output. Say hello. So that's going to call that method. And if we run this. Let's see that we get the hello from the service. So now the service is being set by the property. And again this is the least optimal way. The next preferred way would be via setter. And here we can say private greeting service. And here I can say create a setter for that. And then. And in this case, we want to go ahead and take the greeting service that is being set and say greeting. The problem here is if this class was instantiated, but you don't set the greeting service, you get a null pointer error there. So let's set that up to return. And now let's go ahead and write a test for that. And again here we'll set up the setter inject controller. And then our setup method will initialize that. And again the problem here is if we don't actually set that it would get a null pointer. So I can actually demonstrate that. So this will run okay. But if I forget to run that setter. So let's say I forgot to do that. Now if I run this, I'm going to get a null pointer error. So we can see that pops up pretty quick. So definitely a poor side effect of using both property injected and setter injected. Both suffer from that same thing. Now if we use a constructor. I just corrected the typo there in the name of the constructor injected controller. So here, because we're using constructor I can say private final. And notice I'm in all cases I'm using the interface and not the impulse. So I'm using the interface and not the hard type. And here I can say constructor that is going to create. Now I can only instantiate this class if I pass in a greeting service. Let me minimize that tool window. And finally, we'll also create a test for this. We'll do a setup and also test that. Here in the controller equals new instructor injected controller. And this is also going to take in the new service. So what I'm doing here is even though I'm using a constructor and I'm setting things up in the test, I'm really mimicking what the spring framework would be doing. So in this case spring would be creating that constructor injected controller using the constructor, of course, but then providing it an instance of that. And in this case I don't have another instance. If I had one, I can inject it there. But here I'm creating a new instance, which is perfectly fine for our purposes. So now if I do a system controller say hello. You can see here this is going to run because everything gets constructed properly for us. So what I've shown you in this video is three different controllers, one by property, which is the least preferred way of doing dependency injection. A setter injected controller that's probably the most least preferred. Not as bad as property than constructor. Injected the constructor injected controller. This is really the way you want to be doing dependency injection with a final property and initializing it, and in the constructor itself. And here we could even do an assertion to throw a runtime error if they passed in a null property for greeting service. Probably not a bad idea, and if memory serves me correct, spring would fail this. So here we are doing it manually and not checking for null. And I'm fairly certain that spring will fail if it cannot find the component, or if you're trying to pass in a null component. Everything works properly here, and what we are going to be doing coming up in the course will be exploring how we can do the same operations, but by using the spring framework.

## 37. Dependency Injection using Spring Framework
In the previous video we went through and manually set up our dependency injection. So on the screen here you can see the JUnit test that I have. And basically we are managing the dependency injection. Our classes are set up for dependency injection. But through the JUnit test we are managing it. So what we want to do is refactor our tests and our project, our little project here to go ahead and start using dependency injection. So if we wanted to do the original controller that we started working with can see here that is initializing the greeting service internally. So we would have to make significant changes to that class, which is something that we really don't want to be doing. So here I'm not going to modify the original controller, but the others the constructor property and setter injected controllers I am. So the first thing that we want to do is come in and annotate this with a service annotation. So this is one of the spring stereotypes that says, hey, this is a spring component. So this is one way that we can create a spring component is by annotating it with a spring stereotype. In this case, we are using service indicating that this is supposed to be a service conceptually. Now here what we can do is also annotate this as a controller. So this now says that this is a spring managed component, and in this case with the constructor injected controller, because it has a constructor of the greeting service and only that constructor spring will know that we need to go ahead and auto wire that automatically. However, in this case, if I come in and do the same thing for this controller now, spring doesn't know what to do. So it could do some inspection here. So we need to drop it a hint here. And we want to use the auto wired annotation to say that we want this to be injected. And you see their intelligence is giving us an alert saying field injection is not recommended. I think I've covered that in the course. It is not recommended. It is a way that you can utilize it. I do not recommend it though, so. But I am going to cover that in the course just so you can see the contrast between different ways of wiring things up. And then finally here this is also one of our controllers. By annotating it with the controller, this now becomes a spring managed component. And in this case, because we do have the setter, we can annotate either the property or the setter. So let's go ahead and do the property. Sometimes that gets a little more clear if you have a larger service. So we'll go ahead and annotate that. And we're getting a warning on this. So here you can see it's asking us to create a constructor. We're not going to do that. It's a suggestion. So even IntelliJ knows the proper way to to go through and do things. So now I've updated my different controllers. And let's come in and take a look at the tests test themselves. So let's come to work from the property up. So this is a property injected controller. And what I can do here is I should be able to comment out before each and just say auto wired and also annotate this as a spring boot test. So now I'm going to tell JUnit that I want the spring context to be loaded. And when the spring context runs, it'll look at the default application context as it's configured for application. Here we're not overriding anything. And because the main class is here, spring is going to do what's called a component scan. Now looking at and that package in a package below it, I see a lot of people make mistakes where they put something in one of these other packages. So make sure that you're putting it there. You can tell spring to look elsewhere outside of that directory structure, but by default spring is going to be looking in that core package, that same package that the main class is in or anything below that. So let's go ahead and run this. Verify that the property injection is working and we can see that. Say hello. And you can see here that we did get that injected properly. Now let's come back to our test. And let's go ahead and take care of the setter. And again the setter here we just delete all that. I'll go back to that other test and delete the same stuff. We'll say auto wired. And again we need to annotate this as a spring boot test. Now we can go ahead and run this.  You can see that does the output as expected. And then finally we can verify our constructor. Again I'm going to get rid of that before it auto wired. Come up here at spring boot test. And we'll go ahead and run that. So now you can see that we've refactored everything. So just to recap, what we changed came in here. We annotated the service implementation as a service that indicates the spring that this is going to be a spring managed component. And then also in our three controllers we annotated them as controller, which also indicates that it's a spring managed component. And in this case in the lifecycle of the being, spring needs to create an instance of this to put it into the context. Sees that this has a constructor, so it will execute and inject a greeting service into the constructor so that spring that it needs to create the greeting service first and then inject it into this. And then on the setter, the setter and property. We do need to put auto wired on the property or the setter. So the auto wire could be put on either. And also on the property I only have the property which is not recommended, but now we've turned over all the dependency injection to the spring framework, allowing spring to create and manage the components as needed based on the needs of our application. In this video, I have a small correction to make to the lesson. A guy named Mike on slack pointed out, I made a mistake here and he's absolutely right in this. We have the setter injected controller, and I made a mistake on this because I actually don't use setter injection that often. Or property injection. You can see that IntelliJ has given me a warning here. And the behavior I expected is that the property, the auto wire could be on either the property or the setter, and if the setter was there that spring would use it. Turns out that I am incorrect and we can prove this real quick. So if I come in here and just do a system out and I'll take the recommendation there. So we're just going to say that this method was in fact called. Now if I run the test. We'll see that we don't get that system out. Come down here. We do get the output from the controller, but we do not get or from the service. I mean, but we do not get the system out and the controller from the setter. And if we move this auto wire down to the method itself, like so, and we rerun this, we will see that we do get the system output showing that the controller or the setter in fact was called so very minor. But important distinction here. Placing that annotation on the property will force the property to be utilized. So reflection that happens using reflection which is slow. So definitely not a behavior that we want. And again we're doing the auto setter that utilizes it. I was mistaken in the and previously in the course where I thought that you could put it in either place and that if the setter existed that spring would utilize that. Obviously that is not the case. So we do need to place the auto wire on the setter method and not on the property if we want the setter to be utilized.

## 38. Primary Beans
In this video we're going to take a look at using @Primary beans. So sometimes you want to have more than one implementation. And you want to be able to say that one is the @Primary one. So basically you're going to tell spring when in doubt use this one. So I'm going to come in here create a new Java class. I'm going to call this greeting service primary. And we'll say that this implements greeting service. And in this case we want to return back hello from the primary bean. In this case I'm going to go ahead and mark it as a service. Now if I come on the constructor controller test. So constructor injected let's go ahead and run this test I expect that it's going to fail. I want to show you the failure. So it did in fact fail. And buried down in here we see air creating being name find with let's here caused by no qualifying being of type expected a single matching being but found two. So what spring is saying here is that we are autowiring by type, specifically greeting service and in this case spring saying hey, I found two greeting services in my options to wire into this. I don't know which one to use. So to get around that, what we can do is come up here. At primary now we'll go ahead and rerun this and now we can see say hello work. You can see hello from the primary bean. So by marking that primary every time we ask for a generic greeting service, this is the thing that's going to be injected into that. So at this point we haven't told spring anything different. So everything was asking for a greeting service. So now that I've added this as primary, all of our tests will pick up primary and the previous greeting service implementation. That is not going to be picked up. It'll be picked up into the spring context still, but it's not going to be @Autowired into anything because we have not indicated that. We have told spring at this point that we want to give that greeting service preference over any. So the greeting service primary is being given preference and an @Autowired scenario. So when all things are being equal, we have two instances of the greeting service greeting service simple and greeting service primary. @Primary is annotated with primary. That is the one that spring is going to use if no other conditions are set. So there are other ways that you can control how things are wired. We are going to be looking at those in an upcoming video.

## 39. Using Qualifiers

Now, qualifiers are something that you can use to really control how spring is going to be wiring things up  What we can do here is, let's say in the constructor test, let's say I want that to return back the original instance. So right now this is going to return back the primary being instance. And let's say I want to return back the original one. What I can do here is on constructor injected controller. I can come into here and say qualifier. And by default when spring is going to give the bean a name and that name will be the lowercase of the class name. So if I do greeting service so that's class name but with a leading lowercase. So greeting service apple. Now it will pick up the original instance. So now you can see hello everyone from the base service. And let's say I wanted to do a little bit more control here. And I'm going to add in one more implementation of the greeting service. And say greeting service property injected. Go ahead and add that implements greeting service. Say greeting. In this case I want to say friends, don't let friends. Friends don't let friends do property injection. And now I'm going to go ahead and annotate that as a service. And I'm going to come over here I'm going to add to this qualifier. So now you can see that. And let's come over here to our test and run our test. Make sure that runs as expected. And here you can see how it says friends don't let friends do property injection. And we can actually modify this slightly more. So if I come over here to this now I can come in and give it a name. So now I'm going to call this property grading service to have a slightly different. So I'm giving the being a custom name. I'm just going to copy that and we'll come over to the property injector controller. You can see that went red tells you smart enough to see that is not good. But now I'm going to use that name. So this is custom spring bean name. And we'll run this make sure that is still working. And you can see now that is displaying the proper greeting from the property service. So showed you a couple different ways. By default this bean will get the name of greeting service property injected. I'm overriding it here to property greeting service. So by default that's the default. And here I'm using the default. The qualifier needs to be put on either the constructor or the properties in the constructor, or on the property itself. And then go on the auto wired property. Or in the case of a setter injected it would go on either the property or the setter. I think either one would work and forget if it goes in the method or on the service, on the method or within the property, I forget which there and to solve that, to figure out which set is, we can do a quick test on this command. Create another Java class. Implements greeting service. Setting a greeting. Again this is going to be a service. And here I'm going to call it setter greeting bean. And then on this we can come in here to the setter. I'm pretty sure we can do it right here. Let's find out. Spend some time. I don't have to do it on the setters that often because I typically don't use them. And we call that setter greeting bean. So we got the proper response there, and I'm gonna see if we can just move this to this as well. See if they both work. So that does work. So the qualifier can be put either check that make sure that does in fact work. So no it does not work. So if we do not put it on the property with the auto wire, that does not work. To double check this down. So that's an interesting behavior. So if we do auto wired on the setter the qualifier needs to be on the property itself. So sometimes these minor nuances you need to see. So the setting greeting it looks like it works okay. As long as we don't have the auto wired on the property. So one of those small nuances. So as far as setting that up. So yeah. So if we do the auto wired on the property, then we need the qualifier on the property. If we do the auto wired on the setter, the qualifier can either be on the method or within the method parameters. So I think this is probably the cleanest approach here because usually what happens when you get more complex objects, you'll have multiple properties and multiple things being injected. And typically your getters and setters will be down towards the bottom of the class or elsewhere. Having the components marked like this as a little bit more friendly to developers and developer friendly as far as people reading the code. So double check this still does in fact work. So yes, we are setting a greeting so that still works and you can see that we are getting a warning that field injection is not recommended. So I paused the video and took a closer look at this. I was under the assumption this is what I learned today, and I've been working with spring for a long time. I was under the assumption that auto wire on the property would still use the setter. So what I can do here is set a breakpoint to see if that setter is going to be accessed or not. So I'm going to run into the debugger. And you can see we fall right through that. So that's not desired behavior. But if I move this down to the setter and run this again, if I run it in debugger. Now you see that I hit the breakpoint. So I was mistaken earlier when I said that you could set that either place. I don't typically use setter based injection. I use constructor based injection. That's typically the way I code I used to. It's been quite some time, but I made a mistake on the behavior of spring, so I wanted to correct that. So you can see here that we do hit the breakpoint as expected. But if I annotate the property, even though we have a setter, the setter is bypassed. And I'm assuming that spring is going to be doing reflection underneath the covers to do that. So if you insist on using setter based injection, be sure to be annotating the setter methods and not the property.

## 40. Spring Profiles
This video we're going to take a look at using spring profiles. Profiles are a way that you can control what beans are, a way that you can control what beans are wired into the context and which ones are not. So here you can see here I have a my one or i18n controller test. I18n is a common abbreviation in the Java community and probably elsewhere to abbreviate internationalization. So here I have a controller. And my goal here is to set things up that this will say a greeting either in English or Spanish depending on which profile is active. So I'm going to set up a profile to control what services wired in. So if I come up here to the controller you can see here my controller, this is going to take in a greeting service. All I've done is added the controller and two services. An English service says Hello World. N and Hello Mundo, and I'm sure I'm butchering that because my Spanish is awful. So these services have been configured, but I have not done any configuration of spring. I just set things up. I don't think we need to see me type another service. So the first thing I want to do is come up here and I'm going to annotate this as a service. So that'll bring that into the spring context. And here I also want to annotate this as a service. Now I want to be able to get one of these services in. So I'm going to name this I. Just called it i18n service. And I'm actually going to do the same on both of them. And here. Let's make this final. So set up the constructor for that and we'll use a qualifier. To wire n one that's been set up with i18n service. So I don't need both braces. So what's going to happen now if I run this? If I run a test, let's make this a spring boot test. And we're going to have spring to auto wire that in. So it's going to auto wire an instance of the controller. Now when I run this what's going to happen. You should be able to guess at this point I've got two services that qualify. And we will get an exception saying that we failed to load context. I resize this a little bit. So we have Spanish greeting service. So we're getting a conflict there. I see something that makes sense. Notes. Annotations. Specified IATA service for being class conflicts with existing non compatible being definition of same name and class.  So English greeting service. So it's said that we're wiring in two beans and naming them the same. So that is what the spring context is complaining about. Which is true because both services are being wired in. And the purpose of this video is to show you how to utilize spring profiles. So here's our English service. And we can do is come up here, annotate this with profile. And I'm just going to call that on and do the same here. So now that specifies these and a profile n or s depending on which one is that. Now for the tests we can do here is we can come in and say active profiles. Files, so don't use the profile annotation you want to use. Active profiles and active profiles is for the test itself. And this is a testing annotation. And we're going to say on. And let's go ahead and run this. Now make sure that actually runs. So let's see here hello world end. So that is our expected result. And let's take a look at the Spanish test. So we have spring boot test. And here we need to set the active profile. To yes. So now when we set it to yes what's going to happen I minimize this window here. It's going to happen with ES. It will see the Spanish greeting service because we set the profile of ES on it and the English greeting service because it's in a different profile that is not active. This will be completely ignored by the context. So now when we run the ES, we should get the Spanish. So now that that works. So just to recap what we changed here. We have a controller with a greeting service. We're putting a qualifier on that of i18n service. And we have two services that have been designated as that name. And what we're using is a spring profile. So the active profile of N or ES. So as long as one or the other is active this will perform as expected. So when you have the profile on active this one will get wired in ES. This one will get wired in. So the spring profiles is very very powerful very powerful and very handy way to bring in features. Activate features, change features within your spring application. And we will be seeing profiles used a lot throughout the course.

## 41. Default Profile
Okay. Right now, our two internationalization controllers, our services, I should say, will only function if one of the active profiles is active. If neither is active, we are going to get an error. That's because we don't have a component that spring can wire up. So if I comment out the active profiles here on the English test and run it, we'll see that we get an exception. If I come down here, we can see that we get no qualifying bean of type greeting service because it is looking for a greeting service that matches. This i18n service tag. So right now spring cannot find that. And what we can do here is there's a special profile that spring has. It's called default. The default profile is active when no active profiles are available. So it might be a little confusing at first. So if no other profiles are available, if you use default on your beans, then that will become available. So right now, the way this is configured, this service will only be brought into the context if the profile on is available. But if we wanted to come in with no profile, what we can do is come up here, say default also, so we can assign more than one profile. And the default profile is going to be available when there are no other active profiles of what we're saying. In this case, if no profiles are available, allow the profile to work. Can come in here, verify this test and we can see now hello world again. And then that will also affect our Spanish greeting service I'm going to minimize this. So this will still function normally right now. But if I come to the test actually. So if I run it right now because ES is active we will get the Spanish greeting. But if I comment this out and run it again, it's going to happen is we'll get the English greeting Screaming because we are saying that the default is English, even though here we set up a Spanish test. But if we return back to using the English profile or Spanish profile, we will get the Spanish greeting again. So the default profile is very, very handy and powerful to use when you have scenarios that you want to support active profiles and non-active profiles. And again here the change that I made was important. On the service. You can have multiple profiles active or available for a service. You can say that the service is going to be in multiple profiles. In this case I'm saying on and then also the default which is special profile saying if no other active profiles are there, this is going to be the default that gets wired into the spring context. So I left it just off. I'd be there all the time and would conflict with the other service beans. Again, this default is a very handy tool and very important to be aware of that is available in your toolbox when you are wiring up spring components for different profiles. 

## 42. Spring Bean Lifecycle
In this video we're going to talk about the spring bean lifecycle. So we've been looking at dependency injection. But the way the spring framework actually creates and manages spring beans they go through a very specific lifecycle. So you can see here I've got a little flowchart as to how things go through in Spring Framework. First thing of course is going to instantiate the class. Once the class has been created it's going to populate properties. So any properties that you have it's going to set those up. Then we do have some interfaces that we can implement if they are there. If you've implemented these interfaces spring will execute these methods. So you can see here call the set bean name of being aware. Bean name aware. Then we have the bean factory aware the application context aware. And then we have pre initialization. And this is going to be done with post bean processor. So these are callbacks that we can hook into. And then finally we get it to after properties set of initializing beans. We can also provide custom initialization methods. One of the last things is the initialization. So these are all steps that the bean goes through before it is ready to use. So spring is going to go through and perform all these steps on every bean in the context before the application is considered ready for use. So it gives you a number of places to hook in. You do have a lot of control and flexibility over this. To be honest, it's very, very rare that you need to hook into this. There are use cases that you get into, but it's fairly rare that you need this much control out of how the beans are being created. When you do it, you're usually doing something a little complex. But I think the key takeaway is to have the awareness that this is there. It is there available for you to use when you need it. So here, this is just the bean becoming ready for use. There is also a lifecycle for the beans to be terminated. So when the container is being shut down the application has received some type of shutdown event. There is a lifecycle for the shutdown, so you can annotate a beam method with the annotation app pre destroyed that will get annotated. And then you can also implement a disposable bean interface that provides a destroy method so that the annotation will come first and then the destroy method. And at that point we call that being terminated. So we did talk about some of the interfaces. Spring does have two interfaces you can implement for callback events. You have the initializing bean which gives you after property set. So that is a method that would be executed if you implement that interface. And then we have the disposal being. So we talked about that being one of the last things that gets called before the bean is terminated. You do also have a couple annotations. And these were in that flowchart as well. We have post construct. So the bean has been constructed but it has not been returned to the requesting object. And then we also have pre destroy. So if you remember in the termination workflow this was called just before the interface method is called. And Then we have been post-processors. These can be convenient if you're working with some type of third party object that you need to create and put into your context, and need to update it somehow, like setting configuration properties and typically a non spring managed component. So this gives you some capabilities. The main takeaway you want to have here on the post bean processors, these are called globally. So basically you get this event and it's going to look through. If you implement it you get this called for every object or every bean in the context. So if you have 100 beans it will get called 100 times. So the way you typically implement this, you're probably looking for a specific bean, a type of bean that you need to work with. So you'll do some type of type checking before you try to work with it. And I do have to use this now and then. It's not frequent, but you do have to tap into that. And then finally spring also has over 14 aware interfaces. So these extend interface literally called aware. So they are extensions of that. There are 14 of them. These are largely used within the spring framework. So if you're developing spring applications it's going to be rare that you use these. But be aware. So if you do have a use case, you do get into cases where you do need to tap into these. These can be very handy. Again it's rare very rare that I've had to use it. I've worked with a lot of spring applications, and it's pretty rare that I do need to work with them. These are some of the current ones I'm not going to go through. Everyone leaves the slides here for your reference. You can see there's a lot of them, a lot of different events that we can tap into. And not all of these will be available for every application. But if you do need to tap into one of these to do something that these are really, really edge cases, but be aware that they are available there for you if you do need to use them, they are available. So like everything in spring, that gives you a lot of capabilities, a lot of ways to do customizations.

### Spring Bean Lifecycle
Instantiate → Populate Properties → Call `setBeanName` of `BeanNameAware` → Call `setBeanFactory` of `BeanFactoryAware` → Call `setApplicationContext` of `ApplicationContextAware` → Preinitialization (Bean `PostProcessors`) → `afterPropertiesSet` of Initializing Beans → Custom Init Method → Post Initialization (`BeanPostProcessors`) → **Bean Ready to Use**
Container Shutdown → `@PreDestroy` annotated method → Disposable Bean’s `destroy()` → **Terminated**

### Callback Interfaces
- Spring has two interfaces you can implement for call back events
    - `InitializingBean.afterPropertiesSet()`
- Called after properties are set
    - `DisposableBean.destroy()`
- Called during bean destruction in shutdown

### Lifecycle Annotations
- Spring has two annotations you can use to hook into the bean life cycle
- **`@PostConstruct`** annotated methods will be called after the bean has been constructed, but
before its returned to the requesting object
- **`@PreDestroy`** is called just before the bean is destroyed by the container

### Bean Post Processors
- Gives you a means to tap into the Spring context life cycle and interact with beans as they are
processed
- Called for all beans in context
- Implement interface **`BeanPostProcessor`**
    - **`postProcessBeforeInitialization`** - Called before bean initialization method
    - **`postProcessAfterInitialization`** - Called after bean initialization

### ‘Aware’ Interfaces
- Spring has over 14 ‘Aware’ interfaces.
- These are used to access the Spring Framework infrastructure
- These are largely used within the framework
- Rarely used by Spring developers
- Review extensions of the Aware Interface for current interfaces

| Aware Interface | Description |
| --- | --- |
| `ApplicationContextAware` | Interface to be implemented by any object that wishes to be notified of the
`ApplicationContext` that it runs in. |
| `ApplicationEventPublisherAware` | Set the `ApplicationEventPublisher` that this object runs in. |
| `BeanClassLoaderAware` | Callback that supplies the bean class loader to a bean instance |
| `BeanFactoryAware` | Callback that supplies the owning factory to a bean instance. |
| `BeanNameAware` | Set the name of the bean in the bean factory that created this bean |
| `BootstrapContextAware` | Set the `BootstrapContext` that this object runs in. |
| `LoadTimeWeaverAware` | Set the `LoadTimeWeaver` of this object’s containing `ApplicationContext`. |
| `MessageSourceAware` | Set the `MessageSource` that this object runs in |
| `NotificationPublisherAware` | Set the `NotificationPublisher` instance for the current managed resource instance |
| `PortletConfigAware` | Set the `PortletConfig` this object runs in. |
| `PortletContextAware` | Set the `PortletContext` that this object runs in. |
| `ResourceLoaderAware` | Set the `ResourceLoader` that this object runs in. |
| `ServletConfigAware` | Set the `ServletConfig` that this object runs in. |
| `ServletContextAware` | Set the `ServletContext` that this object runs in. |


## 43. Spring Bean Lifecycle Demo
In this video we have a demonstration of the spring lifecycle. So what I have here is a kind of a hodgepodge of a number of different things, all mashed together, showing you all the capabilities, all the lifecycle capabilities of how a bean is constructed. So just do a quick code review here. We are implementing the various spring interfaces. So we have initializing bean the disposable bean bean name aware bean factory aware application context aware and also being post-processor. So these are all things that we talked about in the previous lecture. I know it's a lot of material. I'm going to go through this somewhat quickly. Primary goal here is to give you a reference. So if you do need to tap into it, you do have lecture and resources to go back through. So you can see how this is implemented. So one of the first things we have here is we have the constructor of that bean. So if I did have something that I wanted to do dependency injection with, I could do so here, Here I have a very simple no args constructor that takes that in, but I am going to output a message for this. This demonstration being, I am prefixing everything with two hash tags so that we can see this in the console. So this is really just going to be a demonstration to the console. So the first thing that happens when spring creates this component of course it's going to call a constructor. So that is going to create the Java object. So now we're going to have an object that we can work with. Now I'm going to scroll up a little bit. We have a private property for Java version. Spring out of the box does have a number of properties set within its context. This here. We haven't covered this in the course yet. We will be getting into properties. That is a whole section worthy topic. But here I'm doing value and I'm using what's called a spring expression or SPL spring expression language. So dollar sign bracket and I'm giving it the property name of that property in the context. So I actually had to run the debugger. Bring that up. Look at the properties to see what was going on. I grabbed this one just to demonstrate this, because one of the first steps of initializing the beans is to go through and set the properties. So this is a property from the environment. We get the Java version. And you can see on line 27 I'm saying output property set and the Java version. So you should see your Java version there. And then the next step is we are calling the bean name aware interface. So we are going to get the bean name. So that'll output that value. And then we get the bean factory aware interface. And then I'm just doing a message here saying that we did get the bean factory output, the application context where that's going to come after the bean factory. And then we have the annotation for post construct. So this is going to be called after the processing has completed on this. But before it is this bean is given to any dependent properties. And this is kind of an odd spot for it in my mind. But you do have the after property set that comes in at number six. The annotated method pre destroy. So basically everything up to here is going to be an initialization. Going down we will start getting into the destroy method. So we have destroy disposable. So remember the annotated method will come before that. And then the destroy method will come just before everything is terminated. Now these two here are a little bit of out of sequence. These are methods that are going to get called because we implemented an interface. These are going to get called for. Every object in the context of these two methods will be called before the destroy methods. So this is part of the startup. So spring is going to go one of the very last steps is going through and initializing it. And here if I  wanted to providing these two methods here I could see if this is an instance of life cycle. Don't actually do that right now. So yeah, just make a little change here on the fly instance of lifecycle demo being say lifecycle demo bean equals. So we're going to have to do a cast there. And I cringe every time I say a cast, but no way around it in this case. So you can see here we have the before in it. We want the before in it like so. No, I don't need to do the print line. I'm already doing that. Sorry about that. It's a little bit on the fly here. We can do the exact same thing. So now we're hooking into those two methods. So I did mention that in the review. I decided to change this on the fly so we can see an example of that. So now I'm just going to run the test. And let's take a look at the console output. May resize this. And we're still getting some complaining in the context. So here's our application startup. We can see here putting out a lot of chatter here. So you can see I'm in the constructor. Do the property set. You can see the bean name aware. So it is lifecycle demo bean bean factory where application context aware post construct after property set. Now you can see here remember the processors. These are going to go through for every bean every bean that we have in the context. So there's a lot of spring framework beans that are getting created for us. You can see like a test executor, things like that. So there are things within spring that you do get out of the box, especially with spring boot environment. So you can see here. Then we get some cheddar. These are from the controller test. And I didn't see the life cycles. Let me pause that. I'm going to pause this for one second. So I did a little bit of on the fly there changing and took a quick look at it. What was happening was these two post-process before initialization and after initialization. What happens is this being does not get picked up by the processor, so it doesn't do itself. So I made a quick change. I added to the my controller being the before and after init. So the two same methods that we previously had there. I carried them to my controller and then for the demo, rather than looking at the lifecycle demo, being now looking at my controller, same logic, I could probably do the B name as well. Either one will work after initialization, just double checking that. So when I run this You can see here we get through the different properties after property set. Now we get into the post-process before and after initialization. These are the output from the controller. So that controller being we are calling methods on that controller being. This gives us control to interact with other beings. And a lot of times it's exactly what you'll need to do. You'll either need to look at the type or a bean name, and then apply some type of logic. Here I'm just doing a system out, and then we can see that we continue processing the beans, and we get down here, and then we get into the destroy. Finally seven and eight, the destroy method. And then it's well, disposable bean is called and then the process terminates. So very simple demo a lot in here. I will be checking this into GitHub. And again it's pretty rare that you need this much control. But it is there for those edge cases when you do need to use it.

