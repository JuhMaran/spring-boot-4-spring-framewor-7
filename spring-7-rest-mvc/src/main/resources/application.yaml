# application.yaml
spring:
  application:
    name: spring-7-rest-mvc
  flyway:
    enabled: false
  docker:
    compose:
      enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
    user:
      name: user1
      password: password
  # Java Virtual Threads
#  threads:
#    virtual:
#      enabled: false

#  jpa:
#    properties:
#      jakarta:
#        persistence:
#          schema-generation:
#            scripts:
#              action: drop-and-create
#              create-source: metadata
#              drop-target: drop-and-create.sql
#              create-target: drop-and-create.sql

logging:
  level:
    web: trace
    guru.springframework.security: trace
#    org.springframework: debug